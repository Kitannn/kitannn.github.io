# v2.kitannn.com (classic portfolio)

The main site at kitannn.com is now the `portfolio-v2` repo. This repo is still the **content source**
(`data.js`, `images/`, résumé) — v2 copies it in with `sync.ps1`.

Portfolio of Henry Tan. Static site on GitHub Pages — edit content in `data.js`.
